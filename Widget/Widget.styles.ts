import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  storyList: {
    flexGrow: 0,
  },
  contentContainer: {
    gap: 5,
    paddingHorizontal: 10,
    paddingVertical: 5,
  },
});
